import MarketingPageShell from '../components/MarketingPageShell';

const FeaturesPage = () => {
  return (
    <MarketingPageShell
      title="Core Features"
      subtitle="Explore real-time messaging, Redis-backed scaling, and live markdown collaboration."
    />
  );
};

export default FeaturesPage;
